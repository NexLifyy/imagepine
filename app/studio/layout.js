export const metadata = {
  title: 'Image Studio – Edit, Resize, Rotate & Flip Images Online | ImagePine',
  description: 'All-in-one browser image studio. Resize, compress, rotate, and flip photos with live before/after comparison. 100% private, no uploads.',
  alternates: {
    canonical: '/studio',
  },
};

export default function StudioLayout({ children }) {
  return children;
}
